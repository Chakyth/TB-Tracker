const db = require('quick.db')
const Discord = require('discord.js')
module.exports = {

name: 'setup remind',
description: ' Setup the remind channel' ,

execute(message, prefix, client, guild, args) {
}
}