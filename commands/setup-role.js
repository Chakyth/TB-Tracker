module.exports = {

name: 'setup-role',
description: 'Setup the Channel' ,

execute(message, prefix, client, guild, args) {
const db = require('quick.db')
    
}
            
            
            
}

